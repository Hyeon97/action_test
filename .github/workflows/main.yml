name: GitHub Actions Demo
on: [push]
jobs:
  CI-TEST:
    name: 테스트 및 빌드
    runs-on: ubuntu-latest
    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      - name: npm install test
        run: npm install
      # - run: npm run lint
